import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {}
});





// const useCreamPuffs = async () => {
//   const results = await CreamPuffRepository.instance.findByYear("2021");
//   return results.map(
//     x => new WeeklyMenuItemViewModel(x)
//   );
// }

// export const creamPuffContainer = createContainer(useCreamPuffs);

// creamPuffContainer.provide();


